{
  "name": "digitalverse-redesign",
  "version": "2.0.0",
  "private": true,
  "description": "DIGITALVERSE — scrollytelling site with admin panel and CRM on Cloudflare Pages + D1",
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && wrangler pages dev --binding SESSION_SECRET=dev-secret-change-me",
    "db:init": "wrangler d1 execute digitalverse --remote --file=./migrations/0001_init.sql",
    "db:init:local": "wrangler d1 execute digitalverse --local --file=./migrations/0001_init.sql",
    "deploy": "npm run build && wrangler pages deploy dist"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "allowScripts": {
    "workerd@1.20260907.1": true
  }
}
